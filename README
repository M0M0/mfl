MFL library.
Graphics and utility functions are in the namespace / folder 
with the same name.

graphics/ are mostly dependent on the 'glm' library.

Deprecated classes are in deprecated/ and do are not in a specific 
namespace. It just contains some Windows classes and other random 
utilities.
